export const metadata = {
  title: 'Plan | XFlowUp',
  description: 'Manage your plan and upgrade options.',
};

export default function PlanLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
